namespace TaskEngine.Desktop.Views.Components;

/// <summary>
/// Code-behind for <c>ConcludeTaskModalView.xaml</c> - intentionally empty besides
/// <see cref="InitializeComponent"/>: every behavior lives in the bound
/// <c>ConcludeTaskModalViewModel</c> (see that view model's own doc comment).
/// </summary>
public partial class ConcludeTaskModalView : ContentView
{
    public ConcludeTaskModalView()
    {
        InitializeComponent();
    }
}
